
import * as RefreshActions from '../action-types/refresh-action-types';
// after making an employee, set this so that it can
// reload employees/restaurants after its created
const intialState = { refresh: false }
export default function refresh(state = initialState, action) {
  // switch(action.type) {
  //   case
  // }
}
